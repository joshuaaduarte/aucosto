// Deprecated. Live elapsed-time counter for the old Rhythms page (now a
// redirect to /app). Superseded by the hub check-in cards. Kept as an empty
// module (no `rm` in this workflow); safe to delete.
export {};
