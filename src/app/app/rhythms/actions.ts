// Deprecated. Rhythms no longer have a dedicated page (it redirects to /app);
// the hub cards drive rhythm sessions through the /api/rhythms route and the
// service layer directly. These page-scoped server actions are superseded.
// Kept as an empty module (no `rm` in this workflow); safe to delete.
export {};
