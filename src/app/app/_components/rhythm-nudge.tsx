// Deprecated. The generic "time for X rhythm?" hub nudge was replaced by the
// morning/bedtime check-in cards in `rhythm-hub-card.tsx` when Rhythms became
// hub-only connective tissue rather than a standalone tool. Kept as an empty
// module (no `rm` available in this workflow); safe to delete.
export {};
